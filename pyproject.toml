[project]
name = "northstar-finance-os"
version = "0.1.0"
description = "End-to-end finance transformation / AI-native ERP portfolio POC for a fictional multi-entity healthcare group."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["06-python/tests", "07-api/tests", "08-accounting-automation/tests"]
